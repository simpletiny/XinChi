<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="commonMapper">   
    <select id="findRecords" parameterType="String" resultType="Map">
    	${value} 
	</select>
	<select id="genPK" parameterType="String" resultType="String">
    	${value} 
	</select>
	<select id="insertBySql" parameterType="String" resultType="String">
    	${value} 
	</select>
	<select id="executeBySql" parameterType="String" resultType="String">
    	${value} 
	</select>
</mapper>