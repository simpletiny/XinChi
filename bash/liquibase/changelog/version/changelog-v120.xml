<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
	xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd
        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">
	<changeSet id="1" author="simpletiny">
		<sql>
            <![CDATA[
           		CREATE OR REPLACE VIEW view_score_from_last_month AS
				    SELECT 
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4),
				            IFNULL(A.adult_count, 0) * SUBSTRING_INDEX(A.product_value, ',', 1) + IFNULL(A.special_count, 0) * SUBSTRING_INDEX(A.product_value, ',', - 1),
				            0)) AS score,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND 11 > SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1),
				            (IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)),
				            0)) AS first_people,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND 11 > SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1),
				            IFNULL(A.adult_count, 0) * SUBSTRING_INDEX(A.product_value, ',', 1) + IFNULL(A.special_count, 0) * SUBSTRING_INDEX(A.product_value, ',', - 1),
				            0)) AS first_score,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND (SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1) > 10
				                AND 21 > SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1)),
				            (IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)),
				            0)) AS middle_people,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND (SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1) > 10
				                AND 21 > SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1)),
				            IFNULL(A.adult_count, 0) * SUBSTRING_INDEX(A.product_value, ',', 1) + IFNULL(A.special_count, 0) * SUBSTRING_INDEX(A.product_value, ',', - 1),
				            0)) AS middle_score,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1) > 20,
				            (IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)),
				            0)) AS last_people,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4)
				                AND SUBSTRING_INDEX(LEFT(A.confirm_date, 10), '-', - 1) > 20,
				            IFNULL(A.adult_count, 0) * SUBSTRING_INDEX(A.product_value, ',', 1) + IFNULL(A.special_count, 0) * SUBSTRING_INDEX(A.product_value, ',', - 1),
				            0)) AS last_score,
				        LEFT(A.confirm_date, 7) AS confirm_month,
				        A.sale AS sale_number,
				        SUM(IF(A.confirm_flg != 'N'
				                AND (A.final_type IS NULL
				                OR A.final_type != 4),
				            (IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)),
				            0)) AS sum_people
				    FROM
				        budget_order_view A
				    WHERE
				        A.standard_flg = 'Y'
				            AND A.confirm_date >= DATE_FORMAT(CURDATE() - INTERVAL 1 MONTH, '%Y-%m-01')
				            AND confirm_date < CURDATE()
				    GROUP BY LEFT(A.confirm_date, 7) , A.sale;

             ]]>
			<comment>CREATE VIEW view_score_from_last_month</comment>
		</sql>
	</changeSet>
	<changeSet id="2" author="simpletiny">
		<sql>
            <![CDATA[
				CREATE OR REPLACE VIEW view_score AS
				    SELECT 
				        SUM((IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)) * A.product_value) AS score,
				        SUM(IF(LEFT(A.confirm_date, 7) = LEFT(CURDATE(), 7),
				            (IFNULL(A.adult_count, 0) + IFNULL(A.special_count, 0)) * A.product_value,
				            0)) AS month_score,
				        A.sale AS user_number,
				        A.sale_name AS user_name
				    FROM
				        budget_order_view A
				    WHERE
				        A.standard_flg = 'Y'
				            AND LEFT(A.confirm_date, 7) >= '2023-02'
				    GROUP BY A.sale;
             ]]>
			<comment>ALTER VIEW view_score</comment>
		</sql>
	</changeSet>
	<changeSet id="3" author="simpletiny">
		<sql>
            <![CDATA[
				ALTER TABLE product 
					ADD COLUMN adult_instant_bonus DECIMAL(12,2) NULL DEFAULT 0 COMMENT '成人立奖' AFTER id_type,
					ADD COLUMN child_instant_bonus DECIMAL(12,2) NULL DEFAULT 0 COMMENT '儿童立奖' AFTER adult_instant_bonus;
             ]]>
			<comment>ALTER TABLE product</comment>
		</sql>
	</changeSet>
	<changeSet id="4" author="simpletiny">
		<sql>
            <![CDATA[
				ALTER TABLE sale_order 
					ADD COLUMN product_line VARCHAR(10) NULL COMMENT '产品线' AFTER final_receivable_comment,
					ADD COLUMN product_bonus VARCHAR(20) NULL COMMENT '产品立奖199.2,299.1表示成人立奖和儿童立奖' AFTER product_line;
             ]]>
			<comment>ALTER TABLE sale_order</comment>
		</sql>
	</changeSet>
	<changeSet id="5" author="simpletiny">
		<sql>
            <![CDATA[
				CREATE OR REPLACE VIEW budget_order_view AS
				    SELECT 
				        A.team_number,
				        A.product_name,
				        A.product_pk,
				        A.product_model,
				        A.client_employee_pk,
				        D.name AS client_employee_name,
				        A.independent_flg,
				        A.comment,
				        A.other_cost_comment,
				        A.other_cost,
				        A.fy,
				        A.special_cost,
				        A.special_count,
				        A.adult_cost,
				        A.adult_count,
				        A.receivable,
				        A.days,
				        A.departure_date,
				        A.pk,
				        A.update_user,
				        A.create_user AS create_user_number,
				        B.user_name AS create_user,
				        A.confirm_flg,
				        A.create_time,
				        A.update_time,
				        A.confirm_date,
				        A.budget_confirm_file,
				        A.standard_flg,
				        IFNULL(A.product_manager, C.product_manager) AS product_manager_number,
				        CASE
				            WHEN A.product_manager IS NOT NULL THEN I.user_name
				            ELSE E.user_name
				        END AS product_manager,
				        A.air_ticket_cost,
				        A.product_cost,
				        A.operate_flg,
				        A.lock_flg,
				        A.cancel_flg,
				        A.name_confirm_status,
				        A.passenger_captain,
				        A.receivable_comment,
				        A.treat_comment,
				        F.client_short_name AS client_name,
				        CASE
				            WHEN
				                A.departure_date = ''
				                    OR A.departure_date > NOW()
				            THEN
				                'no'
				            WHEN
				                NOW() >= A.departure_date
				                    AND DATE_ADD(A.departure_date,
				                    INTERVAL A.days - 1 DAY) > NOW()
				            THEN
				                'yes'
				            WHEN
				                NOW() >= DATE_ADD(A.departure_date,
				                    INTERVAL A.days - 1 DAY)
				            THEN
				                'back'
				        END AS status,
				        A.product_value,
				        A.receivable_first_flg,
				        A.do_confirm_date,
				        A.assistant_number,
				        G.user_name AS assistant_name,
				        A.sale,
				        H.user_name AS sale_name,
				        A.final_type,
				        A.raise_money,
				        A.raise_comment,
				        A.reduce_money,
				        A.reduce_comment,
				        A.complain_money,
				        A.complain_reason,
				        A.complain_solution,
				        A.final_status,
				        A.final_confirm_file,
				        A.final_voucher_file,
				        A.hotel_comment,
				        A.as_adult_flg,
				        A.final_receivable,
				        A.final_receivable_comment,
				        A.product_line,
				        A.product_bonus
				    FROM
				        sale_order A
				            LEFT JOIN
				        user_base B ON A.create_user = B.user_number
				            LEFT JOIN
				        product C ON A.product_pk = C.pk
				            LEFT JOIN
				        client_employee D ON A.client_employee_pk = D.pk
				            LEFT JOIN
				        user_base E ON C.product_manager = E.user_number
				            LEFT JOIN
				        client F ON D.financial_body_pk = F.pk
				            LEFT JOIN
				        user_base G ON A.assistant_number = G.user_number
				            LEFT JOIN
				        user_base H ON A.sale = H.user_number
				            LEFT JOIN
				        user_base I ON A.product_manager = I.user_number;
	 		 ]]>
			<comment>ALTER VIEW budget_order_view</comment>
		</sql>
	</changeSet>
	<changeSet id="6" author="simpletiny">
		<sql>
            <![CDATA[
				CREATE TABLE precise_client_employee (
				  name VARCHAR(10) NOT NULL COMMENT '真实姓名',
				  is_verified CHAR(1) NULL DEFAULT 'N' COMMENT '是否实名',
				  type VARCHAR(10) NULL DEFAULT '未知' COMMENT '客户种类',
				  gender CHAR(1) NULL DEFAULT NULL COMMENT '性别',
				  wechat VARCHAR(20) NULL DEFAULT NULL COMMENT '微信',
				  wechat1 VARCHAR(20) NULL DEFAULT NULL,
				  cellphone VARCHAR(11) NOT NULL COMMENT '手机号',
				  cellphone1 VARCHAR(11) NULL DEFAULT NULL,
				  id VARCHAR(18) NULL DEFAULT NULL COMMENT '身份证号',
				  financial_body_pk CHAR(22) NOT NULL COMMENT '财务主体',
				  head_photo VARCHAR(26) NULL DEFAULT 'img',
				  employee_area VARCHAR(10) NULL DEFAULT NULL,
				  employee_county VARCHAR(20) NULL DEFAULT NULL,
				  detail_address VARCHAR(100) NULL COMMENT '详细地址',
				  comment VARCHAR(100) NULL DEFAULT NULL,
				  binding_status CHAR(1) NULL DEFAULT 'N' COMMENT '关联状态',
				  binding_count INT NULL COMMENT '关联数量',
				  self_photo VARCHAR(26) NULL DEFAULT 'img' COMMENT '本人照片',
				  other_photo VARCHAR(26) NULL DEFAULT 'img' COMMENT '其他照片',
				  pk CHAR(22) NOT NULL,
				  create_time VARCHAR(15) NULL DEFAULT NULL,
				  create_user VARCHAR(10) NULL DEFAULT NULL,
				  update_time VARCHAR(15) NULL DEFAULT NULL,
				  update_user VARCHAR(10) NULL DEFAULT NULL,
				  PRIMARY KEY (pk))
				ENGINE = InnoDB
				DEFAULT CHARACTER SET = utf8
				COLLATE = utf8_bin
				COMMENT = '精准客户表';
             ]]>
			<comment>CREATE TABLE precise_client_employee</comment>
		</sql>
	</changeSet>
	<changeSet id="7" author="simpletiny">
		<sql>
            <![CDATA[
				CREATE TABLE precise_employee_binding (
				  precise_pk CHAR(22) NOT NULL COMMENT '精准客户Pk',
				  employee_pk CHAR(22) NOT NULL COMMENT '客户资料pk',
				  pk CHAR(22) NOT NULL,
				  create_user VARCHAR(10) NULL DEFAULT NULL,
				  update_user VARCHAR(10) NULL DEFAULT NULL,
				  update_time VARCHAR(15) NULL DEFAULT NULL,
				  create_time VARCHAR(15) NULL DEFAULT NULL,
				  PRIMARY KEY (pk))
				ENGINE = InnoDB
				DEFAULT CHARACTER SET = utf8
				COLLATE = utf8_bin
				COMMENT = '精准客户关联员工表';
             ]]>
			<comment>CREATE TABLE precise_employee_binding</comment>
		</sql>
	</changeSet>
	<changeSet id="8" author="simpletiny">
		<sql>
            <![CDATA[
				ALTER TABLE client_employee 
					ADD COLUMN binding_status CHAR(1) NULL DEFAULT 'N' COMMENT '是否关联' AFTER employee_county;
             ]]>
			<comment>ALTER TABLE client_employee</comment>
		</sql>
	</changeSet>
	<changeSet id="9" author="simpletiny">
		<sql>
            <![CDATA[
				ALTER TABLE client_received_detail
					ADD COLUMN reject_reason VARCHAR(200) NULL COMMENT '驳回理由' AFTER collecter;
             ]]>
			<comment>ALTER TABLE client_received_detail</comment>
		</sql>
	</changeSet>
	<changeSet id="10" author="simpletiny">
		<sql>
            <![CDATA[
            	 DROP VIEW IF EXISTS received_detail;
					CREATE VIEW received_detail AS
					    SELECT 
					        team_number AS team_number,
					        received AS received,
					        received_time AS received_time,
					        type AS type,
					        confirm_time AS confirm_time,
					        status AS status,
					        comment AS comment,
					        sum_received AS sum_received,
					        allot_received AS allot_received,
					        related_pk AS related_pk,
					        card_pk AS card_pk,
					        card_account AS card_account,
					        create_user AS create_user,
					        create_time AS create_time,
					        update_time AS update_time,
					        update_user AS update_user,
					        pk AS pk,
					        client_employee_pk AS client_employee_pk,
					        client_pk AS client_pk,
					        voucher_file AS voucher_file,
					        collecter AS collecter,
					        reject_reason AS reject_reason
					    FROM
					        client_received_detail
					    WHERE
					        (type <> 'SUM') 
					    UNION SELECT 
					        group_concat(team_number) AS team_number,
					        received AS received,
					        received_time AS received_time,
					        type AS type,
					        confirm_time AS confirm_time,
					        status AS status,
					        comment AS comment,
					        sum_received AS sum_received,
					        allot_received AS allot_received,
					        related_pk AS related_pk,
					        card_pk AS card_pk,
					        card_account AS card_account,
					        create_user AS create_user,
					        create_time AS create_time,
					        update_time AS update_time,
					        update_user AS update_user,
					        pk AS pk,
					        client_employee_pk AS client_employee_pk,
					        client_pk AS client_pk,
					        voucher_file AS voucher_file,
					        collecter AS collecter,
					        reject_reason AS reject_reason
					    FROM
					        client_received_detail
					    WHERE
					        (type = 'SUM')
					    GROUP BY related_pk;
             ]]>
			<comment>ALTER VIEW received_detail</comment>
		</sql>
	</changeSet>
	<changeSet id="11" author="simpletiny">
		<sql>
            <![CDATA[
            ALTER TABLE reimbursement 
				ADD COLUMN client_employee_pk CHAR(22) NULL COMMENT '销售费用时的客户pk' AFTER delete_flg;
             ]]>
			<comment>ALTER TABLE reimbursement</comment>
		</sql>
	</changeSet>
	<changeSet id="12" author="simpletiny">
		<sql>
            <![CDATA[
            CREATE VIEW view_client_sale_cost AS
			    SELECT 
			        SUM(money) AS money, client_employee_pk
			    FROM
			        reimbursement
			    WHERE
			        item = 'X'
			            AND client_employee_pk IS NOT NULL AND status = 'P'
			    GROUP BY client_employee_pk;
             ]]>
			<comment>CREATE VIEW view_client_sale_cost</comment>
		</sql>
	</changeSet>
	<changeSet id="13" author="simpletiny">
		<sql>
            <![CDATA[
            CREATE OR REPLACE VIEW view_product_profit AS
			   SELECT 
			    LEFT(B.departure_date, 7) AS departure_month,
			    CASE
			        WHEN SUM(A.approved = 'N') > 0 THEN 'N'
			        ELSE 'Y'
			    END AS status,
			    SUM(IF(B.confirm_flg <> 'N',
			        IFNULL(B.adult_count, 0) + IFNULL(B.special_count, 0),
			        0)) AS people_count,
			    SUM(IF(B.confirm_flg <> 'N',
			        IFNULL(B.adult_count, 0) * SUBSTRING_INDEX(B.product_value, ',', 1) + IFNULL(B.special_count, 0) * SUBSTRING_INDEX(B.product_value, ',', -(1)),
			        0)) AS score,
			    B.product_manager_number AS user_number,
			    SUM(IF((A.discount_flg = 'Y'),
			        A.discount_receivable,
			        IF((C.final_flg = 'Y'),
			            C.final_receivable,
			            C.budget_receivable)) - IF(ISNULL(B.air_ticket_cost),
			        0,
			        B.air_ticket_cost) - IF(ISNULL(B.other_cost),
			        0,
			        B.other_cost) - COALESCE(D.payable,0) - IF(ISNULL(B.fy), 0, B.fy) - A.sys_cost - A.sale_cost - IFNULL(A.other_pay, 0) + IFNULL(A.other_receive, 0)) AS gross_profit,
			    B.product_line AS product_line
			FROM
			    team_report A
			        LEFT JOIN
			    budget_order_view B ON A.team_number = B.team_number
			        LEFT JOIN
			    receivable C ON A.team_number = C.team_number
			        LEFT JOIN
			    view_team_payable D ON A.team_number = D.team_number
			WHERE
			    B.final_type <> 4 OR B.final_type IS NULL
			GROUP BY B.product_manager_number , LEFT(B.departure_date, 7) , B.product_line;
             ]]>
			<comment>ALTER VIEW view_product_profit</comment>
		</sql>
	</changeSet>
</databaseChangeLog>







